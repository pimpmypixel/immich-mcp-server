# Immich MCP Server Configuration
# Copy this file to .env and update with your actual values

# Required: Your Immich instance API key
IMMICH_API_KEY=your_immich_api_key_here

# Required: Your Immich instance URL
IMMICH_INSTANCE_URL=https://your-immich-instance.com

# Optional: Port for the MCP server (default: 8000)
PORT=8000

# Optional: Logging level (error, warn, info, debug)
LOG_LEVEL=info

# Optional: Cache TTL in seconds (default: 300)
CACHE_TTL=300